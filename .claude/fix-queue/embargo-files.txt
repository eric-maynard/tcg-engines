# All package lanes (anyadd, makechoices, zeroenergy, cleanup466, identity,
# multiplayer, privacy, trashtiming) completed 2026-08-12; their embargoes are
# released. Add a line only while a lane is ACTIVELY editing a file, and remove
# it when that lane reports done — a stale embargo silently blocks the queue
# (it blocked the Piercing Light blind-benchmark fix after its owner finished).
