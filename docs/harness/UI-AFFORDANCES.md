# Riftbound web client — move-kind → UI affordance table

Every move kind the engine can enumerate for the viewing player must have a
discoverable, human-labelled control where a player looks for it (the card's
own action bar and/or a named sidebar section), and using it must dispatch
exactly that move. This table is backed by
`packages/riftbound-engine/src/__tests__/harness-browser/affordances.test.ts`
(+ `ui-stability.test.ts` for the rune-row / hover-preview invariants)
(gated: `RB_BROWSER_TESTS=1 bun test packages/riftbound-engine/src/__tests__/harness-browser/`),
which drives the live app through `BrowserBackend` and asserts selector + label + dispatched params.

Label rule everywhere: never a raw id (`playFromChampionZone`, `create-token 4`,
`player-1-main-12-…`, `resolvePendingChoice (2 options)`); costs are named where a
choice costs something.

## Moves

| Move kind | Where it appears | Selector(s) | Label rule | Status (before → after this audit) |
|---|---|---|---|---|
| `endTurn` | sidebar **Turn Actions**; `Space` when nothing to pass | `#actionsList .action-btn.primary` "End Turn" | fixed text | OK → OK (test: basics) |
| `concede` | sidebar **Other** | `#actionsList .action-btn` "Concede" | fixed text | OK → OK |
| `exhaustRune` | click the rune; sidebar **Rune Actions** grouped by domain | `.rune-stack .card[data-zone=runePool]`; `#move-group-exhaustRune .action-btn` | "Fury Rune (2 available)" | OK; expanded group could sit below the fold of a 220px panel → panel 340px + scrollIntoView, asserted not clipped |
| `recycleRune` | right-click the rune (ready rune auto-taps first, `+1` float); sidebar group split ready/exhausted | `contextmenu` on rune → `quickRecycleRune`; `#move-group-recycleRune` | "Chaos Rune (ready) (2 available)" | OK → OK (test asserts tap-then-recycle: rune → rune deck, energy +1) |
| `playUnit` (base, single variant) | click / drag hand card → base; sidebar **Play Cards** row per card | `#player-hand .card.playable`; `#actionsList` "Play Unit — <name> to base" | card name + destination | OK → OK |
| `playUnit` (battlefield / Accelerate / other optional costs) | click hand card → **play-options modal**; drag to base *or to a lit battlefield*; sidebar "Play <name> — N play options…" | `#choiceOverlay[data-mode=playCost] .choice-modal-btn[data-variant-idx]`; `.battlefield.valid-target[data-drop-zone]` | "Play to base — 2 energy" / "Play to <bf>" / "Play + Accelerate to base — 2 + 1 energy + fury — enters ready" / "+ sacrifice X" / "+ discard X" / "+ pay N XP" / "+ exhaust legend" | modal OK; **hand→battlefield drag did not exist**, raw `battlefield-…` location in single-variant rows → drag targets lit battlefields, `location` humanised |
| `playSpell` (untargeted) | click hand card = cast; sidebar row **per card** | `#actionsList` "Play Spell — <name>" | card name (+ "(Repeat ×n)" / "(+ additional cost)") | **two different spells collapsed into "Play Spell (2 options)"** → one row per card |
| `playSpell` (targeted / multi-target / up-to-N) | click hand card → **targeting mode**: legal targets glow, banner "Choose a target for X — Esc to cancel", buttons "No target" / "Done (n)" / "Repeat xN" / additional-cost; sidebar "<name> — N targets…" enters the same mode | `body.targeting-mode`, `.valid-target[data-card-id]`, `#targetBanner.visible .target-banner-btn`, `#actionsList [data-target-play]` | banner names the card; glow set == engine target set (asserted) | OK → OK |
| `playSpell` (modal "Choose one —") | printed variant is cast; the engine's `choose-mode` prompt (at play or resolution) shows one button per mode | `#choiceOverlay[data-mode=pending] .choice-modal-btn` | server `optionLabels` → printed bullet; fallback renders the effect ("Counter a spell", "Play 4 1 Might Bird unit tokens with Deflect") | OK since 3355e24; fallback asserted |
| `playGear` | click hand card; drag Equipment onto a unit = play attached | `#player-hand .card.playable`; unit under pointer `.drag-over` | card name | OK → OK |
| `equipCard` ([Equip] a board Equipment, rule 476.1) | **card bar** "Equip [fury] → choose a unit" → targeting over legal units; **drag** the Equipment onto a unit; sidebar **Abilities** "Equip — <gear> — Equip [cost] → choose a unit" | `#actionBar [data-equip]`; `.valid-target`; `#actionsList [data-target-play]` | printed Equip cost + names | **missing entirely (raw "equipCard Serrated Dirk, Void Hatchling" under Other; board click did nothing; not glow-playable)** → fixed, asserted end-to-end |
| `hideCard` (rule 723) | targeting banner extra button "Hide at <bf>"; play-options modal row "Hide at <bf> — facedown…"; drag hand card onto the held battlefield; sidebar **Play Cards** "Hide Card — <card> at <bf>". **Hide is always an explicit choice:** when Hide is the card's ONLY legal use (can't afford / wrong timing for the play) a bare click no longer hides — the card is selected, its battlefield(s) light up and the bar reads "<card> — can't be played right now (…); it has [Hidden], so you may hide it face-down:" with one `Hide at <bf>` button per variant + `Cancel` (`enterHideOnlySelected`); dropping the card on a battlefield where Hide is the only variant asks the same way | `#targetBanner .target-banner-btn`; `#choiceOverlay .choice-modal-btn[data-hide-idx]`; `#actionBar [data-hide-at]`; `.battlefield.valid-target`; `#actionsList` | names card **and** battlefield | **only a sidebar "Hide Card (2 options)" group with "at <bf>" (no card name); no on-card path** → fixed; **single hide variant auto-hid on click** → explicit (asserted in `ui-stability.test.ts`: masked play variants → click dispatches nothing, bar offers "Hide at …", battlefield lit) |
| `revealHidden` | click the facedown card at the battlefield → bar "Reveal (play for 0)" (greyed "— not yet (from your next turn)" the turn it was hidden); sidebar **Abilities** "Reveal Hidden — <card>" | `.bf-facedown .card.card--bf-hidden[data-card-id]` → `#actionBar button`; `#actionsList` | names card | **facedown card click did nothing** → fixed |
| facedown (Hidden) cards on the board (rule 723 / 127) | every seat sees a **card BACK** in the battlefield's row (`renderFacedownCard`: tilted, `HIDDEN` badge, blue edge = yours / red = theirs, 👁 chip when this seat may peek). Hover preview is **seat-aware and server-decided**: the per-seat snapshot (`server/snapshot.ts buildGameSnapshot`) carries the real `definitionId` only for the controller — or a seat holding a rule-127 look grant (`state.visibilityGrants`, e.g. unl-053-219 Scuttle Crab), or anyone once the game is finished (421.4) — and an opaque `hidden-facedown-<bf>-<owner>-<i>` stand-in (`definitionId:""`, `name:"Hidden card"`) otherwise, in duels and vs-Claude alike (goldfish sandbox: unredacted). With a def id the preview shows the real card + ribbon `#previewRibbon` "Hidden — only you can see this" / "Hidden by X — revealed to you" / "— visible in sandbox"; without one it shows a full-size back (`#previewBack`) titled "Facedown card · Hidden — controlled by <name>" and the rule-723 hint — the client never knows more than its snapshot | `.bf-facedown .card.card--bf-hidden[data-facedown="1"][data-owner]` (`data-def-id` present ⇔ this seat may see it), `.card--bf-hidden-mine` / `-theirs`, `.card-hidden-badge`, `.card-hidden-peek`; `#cardPreview #previewRibbon`, `#previewBack`, `.card-preview--facedown` | never a def id / name this seat wasn't sent | **was: owner saw the full face in a dashed box, opponent an unlabelled placeholder; in a human duel the opponent's snapshot leaked the hidden card's id/def/name (facedown zones were only redacted vs Claude)** → fixed; server asserted in `apps/riftbound-app/server/__tests__/snapshot-redaction.test.ts` (owner vs opponent token, vs-Claude, goldfish, Scuttle grant + expiry, game end), client in `ui-stability.test.ts` (own back + peek ribbon; the exact redacted shape renders a back whose hover shows only the back) |
| `playFromChampionZone` | click champion → bar "Play Champion to Base" / "…to <bf>" (2+ cost variants → play-options modal); drag to base / lit battlefield; sidebar **Play Cards** "Play Champion — <name> to base" | `#actionBar [data-champion-play]`; `#actionsList [data-play-cost-card="__champion"]` | "Play Champion …" + destination | **bar button read `playFromChampionZone`; sidebar row `playFromChampionZone base` under Other** → fixed |
| `standardMove` (single unit) | click unit in base → bar "Move to <bf>" per destination + battlefield glow; drag unit → battlefield; sidebar **Movement**. **Battlefield → base (rule 144.4.b):** click a ready unit at a battlefield → bar "Move to Base" + the base row glows (`#player-base.valid-target`), or **drag it onto the base row**; the solo-unit variant is preferred over subset variants (`preferSoloUnitMoves`) | `#actionBar .action-bar-btn`; `.battlefield.valid-target`; `#player-base[data-drop-zone="player-base"].valid-target`; `#move-group-standardMove` | battlefield names / "Move to Base" | OK; **`destination:"base"` moves were enumerated but had no on-card path (only gank/recall were read for battlefield units)** → click + drag, asserted in `ui-stability.test.ts` (bar button, base glow, drag dispatches `standardMove{unitIds:[unit],destination:"base"}`) |
| `standardMove` (multi-unit) | sidebar **Movement** group lists each unit set: "A, B to <bf>" | `#move-group-standardMove .action-btn` | unit names + destination | sidebar-only (no multi-select gesture) — unchanged, labelled |
| `gankingMove` | click unit at a battlefield → bar "Gank to <bf>"; drag bf→bf; sidebar "Ganking Move — <unit> to <bf>" | `#actionBar`; `#actionsList` | names | OK → OK (asserted) |
| `recallUnit` | click unit at a battlefield → bar "Recall to Base"; sidebar | `#actionBar`; `#actionsList` | fixed text | OK → OK |
| `activateAbility` (unit / gear / legend) | **card bar** button = printed "COST: effect" text (one per ability) → targeting when targeted; sidebar **Abilities** "Activate Ability — <card> — <COST: effect> — N targets…"; a printed ability with **no legal move** still shows on the bar, greyed, with the reason ("Already exhausted" / "Can't pay its cost right now" / "Only on your turn" / "Only Reactions while the chain is open") | `#actionBar .action-bar-btn[data-ability-group]`, `[data-ability-disabled]`; `#actionsList [data-target-play]` | printed ability text, never "Activate Ability N" | labels OK since 422f088; **legend with no move was inert (no pointer events, no reason)** → greyed-with-reason; gear bar said "Unit:" → "Gear:/Equipment:" |
| `passChainPriority` | chain overlay "Pass (Space)"; `Space`/`A`; sidebar "Pass Priority"; header "You have priority — react or pass (Space)" (gold when it is the opponent's turn) | `#chainOverlay.visible .chain-pass-btn`; `#sidebarHeader .game-status[data-cursor]` | — | OK; **header said "Waiting for <opponent>" while we held priority** → cursor-aware header |
| `passShowdownFocus` | battlefield showdown panel "Pass Focus (W)"; `Space`; sidebar "Pass Focus"; header "You have focus — act or pass (Space)" | `.battlefield__showdown-btn--pass` | — | OK → OK (+header) |
| `resolveChain` / `resolveFullCombat` / `conquerBattlefield` / `endShowdown` | chain box "Resolve"; showdown panel "Conquer (Q)" / "Cancel"; server auto-resolves combat | existing | — | unchanged |
| chain contents | chain overlay item rows: name, controller, spell/trigger, **what it does: mode text and "→ target names"** | `#chainOverlay .chain-item [data-chain-what]` | humanised effect / chosen mode + target names | **items showed only name/type** → targets + mode shown |
| `resolvePendingChoice` — every prompt type | **choice modal** (`#choiceOverlay[data-mode=pending][data-pending-type]`) titled per type; sidebar mirrors title (`[data-pending-type]`) + buttons (`[data-pending-pick]`); board picks glow and the backdrop is click-through | see below | see below | see below |
| … `opt-in` (rule 583) | Yes / No; title "Use <card> ability? (pay 2 energy, fury)"; the **asking card is shown in the prompt** (art + printed text, `[data-prompt-source]`, hover-previewable) with an orange "If you say Yes: <cost>" box (`[data-optin-cost]`); `describeOptInCost` is exhaustive over every engine cost key (energy · power pips · XP · **Burn N — put the top N cards of your deck into your trash** · exhaust self/other · kill · discard · recycle self / N from hand·board·trash / a rune · spend buff · banish · return to hand · X) and falls back to "pay its <key> cost (see the card)" for an unknown key — never silent; Accelerate-from-trash: "Pay — enters ready" / "No — enters exhausted"; counter ransom | `.choice-modal-btn[data-other-idx]`, `[data-prompt-source]`, `[data-optin-cost]` | cost named in title (short form) and body (full) | OK (+ accelerate/ransom titles); **`{burn:1}` rendered nothing → "Burn 1 — …" (asserted)** |
| … `confirm` (355.13) | Yes / No; title = prompt | same | prompt text | **title fell back to "Choose a card"** → prompt |
| … `choose-target` | card tiles (role=button, keyboard Enter) + board glow, backdrop pass-through; "any number": Done; split damage: one button per split "A 2 · B 1"; RPL "Which effect saves X?" | `.choice-modal-card[data-pick-idx][data-card-id]`, `.valid-target` | card names; tokens get a named fallback tile | OK; tiles were plain divs → role=button/tabindex |
| … `choose-destination` (355.4) | "Base" / battlefield names / "Don't move" (optional) / "(pay additional cost)" | buttons | zone names | title now names the unit |
| … `choose-mode` (355.3/355.8) | one button per mode | buttons | printed bullets / rendered instruction | OK |
| … `reveal-and-pick` (Vision / Predict / look-at-N / reveal hand / discard N / play from trash…) | dedicated renderer (`renderRevealAndPick`): title says in plain words **where the cards are and what happens** ("Mystic Poro — Top card of your deck: keep it on top, or recycle it to the bottom?", "Stacked Deck — The top 3 cards of your Main Deck: pick 1 to draw, the rest go to the bottom", "Sabotage — Goldfish's hand (5 cards): recycle 1 to the bottom of their deck"); a visibility line (`[data-rp-seen]`: looked at privately / X revealed these / your own hand); a one-line instruction (`[data-rp-instruction]`) naming what a click does and the fate of unpicked cards; **one column per looked-at card** = tile + name + a `.choice-modal-btn[data-rp-card]` whose text IS the action for that card ("Recycle to bottom", "Draw it", "Play it — 2 energy + 1 fury" / "(free)" / "(no energy cost)", "Banish it (returns when they hold)", "Discard it", "Put on top or bottom…"); ineligible cards stay visible, dimmed, captioned "Can't pick — not a spell · will go to your hand" (`[data-rp-ineligible]`); optional prompts get a **separate labelled keep/decline button** (`[data-rp-decline]`: "Keep it on top (choose their order next)", "Draw none — recycle all 3 to the bottom", "Don't discard — leave the hand as it is"); `remaining > 1` / `upTo` prompts are **select-then-confirm** (tiles toggle `.rp-picked` + "✓ Verb" badge, `[data-rp-confirm]` reads "Discard 2 picked · 4 others stay in hand", sends `pickedCardIds`); rules hint line (`[data-rp-rules]`). Sidebar buttons mirror as "Recycle <name>" / decline text. No hover layout shift on tiles | `#choiceOverlay[data-pending-type=reveal-and-pick] .rp-pick`, `[data-rp-card]`, `[data-rp-decline]`, `[data-rp-confirm]`, `.choice-modal-card[data-pick-idx]` (tile click = same action for single picks) | action-named buttons, never a bare card name | **was: title "Recycle a card"/"Choose a card to draw", bare card tiles ("click = mystery action"), generic "Decline", no way to answer "one or both" with both** → fixed, asserted for recycle(Vision)/draw+rest/play/banish/discard×2/upTo/opt-in burn |
| … `name-card` (762) | one button per legal name **+ filter box** when > 12 | `.choice-modal-filter`, buttons (scrolling list) | names | hundreds of buttons unfiltered → filter |
| … `choose-player` | one button per seat | buttons | player display names | **"—"** → names |
| … `combat-damage` (465.2.c) | **ordered drag lane** (`combat-assign.js`, loaded by modals.js): header tile "assign N dmg →" then one draggable tile per receiving unit, left → right = the order they take damage; the split is DERIVED from the order by the rules' greedy fill (each unit takes its engine-provided `lethalNeed` before the next gets any, 465.2.c.3/.4; excess — only once everyone is lethal — piles on the last unit reached; later units show **0 · "no damage reaches"**, greyed); each tile shows the big assigned number, "☠ lethal · needs N" / "n/N — survives", ◀ ▶ buttons (+ ArrowLeft/Right on the focused tile, Enter confirms) and HTML5 drag; **Tank pinned to the front / Backline to the back** (`tier` from the payload; a Tank+Backline unit may sit at either end, 465.2.c.8) — an illegal move snaps back with a hint (`[data-cd-hint]` "🛡 X has Tank — it must be assigned lethal damage before any non-Tank unit"); running line `[data-cd-status]` "8/8 assigned · lethal to A, B"; **Confirm — A 3 · B 3 · C 2** sends `resolvePendingChoice{allocation}` (the exact enumerated variant when one matches, else the composed map — engine validates); "Advanced: edit numbers ▸" swaps the numbers for − n + steppers (client mirror of `isLegalDamageAssignment` disables Confirm with the reason, e.g. "must total 4 (now 3)"); works for attacker and defender sides and the 1-unit case; non-combat splits (355.14 `choose-target`+`assign`) keep the per-split buttons. Sidebar still lists the enumerated splits as quick picks | `#choiceOverlay[data-pending-type=combat-damage] [data-cd-tile][data-card-id]`, `[data-cd-amount]`, `[data-cd-left]`/`[data-cd-right]`, `[data-cd-confirm]`, `[data-cd-advanced]`, `[data-cd-step][data-cd-unit]`, `[data-cd-status]`, `[data-cd-hint]`, `[data-cd-total]` | unit names + derived amounts; never ids | **was one button per legal map ("Unit 2 · Other 1", up to 200)** → drag lane; asserted in `harness-browser/combat-assign.test.ts` (greedy 3·3·2, ◀ + drag reorder, Tank snap-back, exact vs composed dispatch, advanced illegal/legal, defender + single unit, non-combat split untouched) + app unit test `server/__tests__/combat-assign.test.ts` (greedy fill / order rules cross-checked against the engine validator) |
| … `weaponmaster-equip` | equipment tiles + "Don't equip"; title names the unit | tiles + button | names | **"No" / "Choose a card"** → fixed |
| … `pay-x` | **stepper** − X + and "Pay N" over the enumerated amounts | `[data-x-step]`, `[data-compose-confirm]` | "X = n" | **buttons labelled "—"** → stepper |
| … `order` (372 die-order / 383.3.d) | **sequence chooser**: click items in order (numbered), "Confirm order (A → B)", "Reset", "Keep listed order"; > 4 items sends the composed permutation (engine validates) | `[data-seq-key]`, `[data-compose-confirm]`, `[data-compose-default]` | item labels / card names; die-order title "<dying card>: Order the replacement effects…" | permutation buttons only → chooser |
| … `order-cards` (386.2 Predict) | same sequence chooser, "first = top of deck" | `[data-seq-key]` | card names | **"—" buttons** → chooser |
| … `pick-many` (373 die-assign / 355.11.b) | **checkbox chooser** with min/max, "Done (n)" enabled only in range, "None" when min = 0; title "<source>: Choose which death this replacement effect applies to (1)" | `[data-check-key]`, `[data-compose-confirm]` | option labels / card names | subset buttons only → chooser |
| trigger order (383.3.d, soft — DESIGN.md §Trigger ordering) | **in-board draggable STACK POPUP** `#triggerOrderPopup` (no backdrop — nothing is blocked): the controller's triggers listed TOP OF CHAIN FIRST with live "resolves 1st/2nd…" numbers, reorder by HTML5 drag-and-drop or ↑/↓ (ArrowUp/Down on a focused row), "Confirm order" → `resolvePendingChoice{orderedKeys}` (= reverse of the display; exact enumerated variant when offered), "Use default" → listed order; header-draggable; removed as soon as the offer is gone. Sidebar keeps a compact hint: "Order your simultaneous triggers … (optional — any other action keeps this order)" + "Reorder N triggers in the chain popup…" + "Keep this order"; needs `pendingTriggerOrder` in the snapshot (server/snapshot.ts) | `#triggerOrderPopup [data-trigger-row]`, `[data-trigger-up]`/`[data-trigger-down]`, `[data-trigger-order-confirm]`, `[data-trigger-order-default]`; `#actionsList [data-trigger-order]`, `[data-trigger-order-open]`, `[data-trigger-order-pick]` | "<Card> trigger — resolves 1st" | permutation buttons in the sidebar → popup |
| sandbox meta (`addToken`, counters, buffs, resources ±, peek dialog) | token `+` panels, meta-actions panel, resource bar ±, right-click deck | existing | — | unchanged (out of scope) |

## Play menu — solo picker (DESIGN.md §Play menu)

| Affordance | Where | Selector(s) | Rule | Status |
|---|---|---|---|---|
| Opponent's deck for Goldfish / VS Claude | `#soloDeckPicker`, directly under the **Opponent** selector (`#soloOpponent` + hint) | native `#soloOppDeckSelect` (source of truth; harnesses set `.value` + `change`) dressed by the rich `.deck-dd` picker; row `#soloOppDeckRow` labelled "Opponent's deck" | entries in order: `mirror` "Same as mine (mirror)" (subtitle/pips mirror the player's current pick), `random-mine` "Random from my decks" (only with saved decks), optgroup **Your Saved Decks** (deck ids), optgroup **Public Decks** (others' public decks), optgroup **Default starter** (`default`); remembered in `localStorage["rb-opponent-deck"]`; sent as `opponent.deck` on `/api/lobby/create`; a server rejection (not yours / not found / illegal) shows in `#soloDeckStatus` and creates nothing | new — test: `harness-browser/solo-opponent-deck.test.ts` (groups + persistence + Goldfish "mirror" ⇒ `#opponent-legendChampion` shows my legend); server: `apps/riftbound-app/server/__tests__/solo-opponent-deck.test.ts` |
| **Goldfish — Passive / Active** (play both seats) | `#soloDeckPicker` **Opponent** selector `#soloOpponent`: options `goldfish` "Goldfish — Passive (auto-passes)" (default), `goldfish-active` "Goldfish — Active (you play both seats)", then the Claude models; remembered in `localStorage["rb-opponent"]`; sent as `opponent:{kind:"goldfish",mode:"passive"\|"active"}` | in game (active only): top strip `#hotSeatBanner[data-seat=player-1\|player-2]` "Acting as **Player 2** (Goldfish — active)" + `#hotSeatSwitchBtn` "Switch seat `Tab`"; `Tab` anywhere outside form fields; the sidebar `#playerSwitcher` buttons; `window.__rbViewingPlayer` follows | ACTIVE: server `hotSeat` — no Goldfish driver, no bot pregame picks; the client auto-switches to the seat that owes the decision (turn / chain priority / showdown focus / prompt owner; pregame: battlefield pick → sideboard lock → mulligan, mine first then the other seat's; the roll winner's go-first choice is offered even when player-2 won) via WS `{type:"switch_seat", playerId}` → that seat's `sync` (board flips perspective; `#player-hand` = that seat's real cards, `#opponent-hand` = `hidden-…` card backs — per-seat redacted snapshot); a manual switch is honoured until the next move frame; Rewind = one action of whichever seat; PASSIVE: unchanged (driver auto-passes; `switch_seat` refused `NOT_HOT_SEAT`) | new — server: `apps/riftbound-app/server/__tests__/goldfish-mode.test.ts` (active: no driver, host socket switch_seat + P2 battlefield pick + both mulligans + P2 move accepted, per-seat hand redaction, rewind 1 step; passive: driver plays back, switch_seat refused, P2 move from host refused) |
| Opponent's deck in a hosted lobby's **Single Player** mode | `#lobbyRoom`, under Select Deck, host only, only while `lobby.sandbox` | `#lobbyOppDeckRow` / `#lobbyOppDeckSelect` → WS `{type:"select_opponent_deck", deck}`; guest card reads "Solo Opponent · <deck>" | same entries / ownership rule; errors arrive as `{type:"lobby_error"}` → status line | new |
| Deck picker rows show the deck's **legend art** | every `.deck-dd-item` / `.deck-dd-btn` (lobby `#deckSelect`, solo `#soloDeckSelect`, opponent pickers) | `.deck-dd-thumb img[src^="/card-image/"][alt=<legend name>]` left of the name (30×30, cropped to the art); entries without a legend (starter / mirror before a pick) show a neutral ⚔ glyph; `option.dataset.legendId` carries the id | same `/card-image/:id` URL as the board; `onerror` degrades to the glyph | new — asserted in `harness-browser/decks-page.test.ts` |

| **Skip animations in practice games** | `#soloDeckPicker`, under the Bo1/Bo3 radios | `#soloSkipAnimToggle` (checkbox) ↔ `localStorage["rb-skip-pregame-animations"]` = "1"/"0" | default OFF (roll overlay shown vs Goldfish / Claude); ON restores the instant start (auto `choose_first`, no `#coinOverlay`); with NO stored value an unattended `navigator.webdriver` browser is treated as ON so playtest drivers never stall on a go-first prompt — harnesses that test the overlay set "0" explicitly | new — asserted in `harness-browser/pregame.test.ts` (pref ON ⇒ Bo1 start reaches the mulligan and `#coinOverlay` never became visible) |

## Pregame vs a bot — roll, battlefield pick, lock-in, Leave (DESIGN.md §Pregame; server `apps/riftbound-app/server/pregame.ts`)

| Affordance | Where | Selector(s) | Rule | Status (before → after) |
|---|---|---|---|---|
| d20 initiative roll is **shown** in solo games | `#coinOverlay.visible` — Bo1: right after Play; **Bo3: only AFTER both battlefields are locked** (pregame phase `initiative`, answer goes on the GAME socket as `pregame_choose_first {choice}`; `chooseTurnOrder()` routes there automatically) | `#duelRoll1` / `#duelRoll2` (server rolls), `#coinResult` "<X> rolled higher (17 vs 4)", `#coinDetail`, `#coinChoose .coin-choose-btn` ("I'll go first" / "Opponent goes first"), `#coinOverlay[data-stage]` = `rolling` → `settled` → `choose` \| `waiting` \| `decided` | ~1.5s dice animation then the result; **human won** ⇒ the go-first/second buttons (lobby WS `choose_first`); **bot won** ⇒ the server already decided (Goldfish / Claude elect to go first, rule 115 — the roll is no longer rigged for the host) and the overlay says "<bot> won the roll and chose to go first", then proceeds after ~1.5s; click anywhere = skip (1st click settles the dice, 2nd proceeds when nothing is left to choose); log: "X wins initiative (17 vs 4)…" / "Chose X to take the first turn." | **was: `_soloAutoStart` auto-sent `choose_first:"self"` and never showed the overlay; sandbox roll rigged so the host always won** → shown, un-rigged, asserted (both branches; deterministic in-page bot-won run: stages, texts, self-proceeds, click-skip < 1s) |
| **Choose Your Battlefield** (Match / Bo3, rule 486.5) shows the CARDS | `#pregameOverlay.visible #bfChoices` | one `button.bf-choice[data-bf-id][data-def-id][data-card-type=battlefield][data-rules-text]` per registered battlefield: `.bf-choice-art img.bf-choice-img[src="/card-image/<id>"]` (landscape, `contain`; `onerror` ⇒ printed-text fallback) + `.bf-name` under it; hover ⇒ shared `#cardPreview` (landscape 420px art + name + full rules text — `.bf-choice[data-def-id]` is a preview surface); `#bfPickHint` "Click a battlefield to lock it in — the choice is final." | never a raw id; text stays accessible via the preview | **was: three text tiles (name + rules text)** → card art, asserted (3 imgs, names, hover preview name/text/landscape) |
| Battlefield **lock-in is final** | same screen after a pick | all `.bf-choice` `disabled`, ours `.selected` + `.bf-choice-badge` "Locked in", others `.bf-choice--dimmed`; `#bfLockedStatus` "Locked: <name> — waiting for opponent…"; client never sends a 2nd pick (`selectBattlefield` guards on `pregameState.battlefieldSelected` / in-flight) | server `selectBattlefield()` refuses a second `pregame_battlefield_select` from the same seat (and late picks after the phase moved on) with `{type:"error", errorCode:"BATTLEFIELD_SELECT"}` — raw WS included; the first pick stands | **was: `.selected` only had `pointer-events:none`, the other two stayed clickable and the server overwrote the selection** → final, asserted (UI render + raw WS error frame; server: `pregame-battlefield.test.ts`) |
| Bot seat makes its own pregame choices | server-side, `runBotPregame()` on game start / human connect / human pick | log lines "🐟 Goldfish picked a battlefield at random: X." / "🤖 Haiku chose its battlefield: X — '<rationale>'" / "🤖 Sonnet: model timed out — battlefield picked at random (X)." then "<bot> locked in a battlefield (X)." | Claude seat: ONE bounded (10s) model call listing its 3 battlefields + both legends → index; failure/timeout/bad index ⇒ pick seeded by the game seed (never the engine RNG); Goldfish: seeded pick; sideboard phase: bot locks with no swaps; mulligan: kept alongside the human's | **was: no hook — a Bo3 vs Goldfish/Claude hung at "Waiting for opponent to choose..." forever** → fixed, asserted (browser: Bo3 vs Goldfish reaches the mulligan; server: `pregame-bot.test.ts` goldfish + mocked Claude incl. timeout → fallback) |
| **Leave match** on every pregame screen | top-right of `#pregameOverlay` (battlefield pick / locked-waiting / sideboard / mulligan / waiting-for-opponent) and of `#coinOverlay` (roll / choose-first / waiting) | `#pregameLeaveBtn` / `#coinLeaveBtn` (`.pregame-leave-btn`, injected by `ensurePregameLeaveButton()`) → `#confirmLeave.visible` (now `z-index:4000` by id — `features.js` injects a generic `.confirm-overlay{z-index:300}` that used to sink it UNDER the pregame overlay) → **Leave** ⇒ `leave_game` on the game WS (pregame ⇒ `abandonPregame`: every client gets `{type:"game_ended", pregame:true, reason}`, sockets closed, session deleted, spawning lobby + code freed) or `leave_lobby` on the lobby WS before a game exists (host ⇒ lobby deleted + guest told `lobby_closed`; guest ⇒ seat reopens, pending roll voided) ⇒ `returnToPlayMenu()`: `#startScreen` + `#lobbyMenu` visible, `gameId === null`, `sessionStorage.rb_game` cleared, solo picker re-armed | never a dead end: a player can always abandon a pregame | **was: no way out of "Choose Your Battlefield" / "Waiting for opponent" short of reloading (and the saved session put you straight back)** → fixed, asserted (button visible + topmost on picker, mulligan, sideboard and waiting renders and on the roll overlay; click ⇒ menu; `GET /api/game/<id>/state` ⇒ 404) |

## Match play — Bo3 lifecycle + concede (`public/js/gameplay/match.js`, server `server/match.ts` / `match-state.ts`; DESIGN.md §Match play)

| Affordance | Where | Selector(s) | Rule | Status |
|---|---|---|---|---|
| Bo3 pregame order: **battlefield → roll → mulligan** | solo Bo3 / hosted Match | `#pregameOverlay #bfChoices` first (hint "First player is decided after battlefields are locked"), THEN `#coinOverlay[data-stage]` (`choose` ⇒ `#coinChoose .coin-choose-btn`), then `.mulligan-btn-keep` | rules 113/486.5 → 115 → 116/117; server `pregame.initiative {kind:"roll", chooser, p1Roll, p2Roll, decided}`; payload `firstPlayer:null` until decided | new — `server/__tests__/pregame-bot.test.ts`, `match-flow.test.ts`; browser `harness-browser/pregame.test.ts` |
| Games 2–3: **previous loser chooses** who goes first | after battlefield pick (+ sideboard) of game 2/3 | `#initiativeStep[data-chooser]` with `#initiativeSelfBtn` / `#initiativeOppBtn` (chooser) or `#initiativeWaiting` | OP convention (115.1.a; 486 silent); bot chooser elects to go first (log "X lost game N and chooses to go first.") | new — `match-flow.test.ts` |
| Used battlefields excluded (rule 486.5) | game 2/3 picker | `.bf-choice[data-used=1].bf-choice--used:disabled` + `.bf-choice-badge--used` "Used this match"; server refuses (`BATTLEFIELD_SELECT` "already used this match") | 486.5 / 486.5.a | new — `match-flow.test.ts` |
| Match chip + **Concede game / Concede match** | sidebar header while playing | `#matchChip` "Bo3 · Game N · a–b"; `#concedeGameBtn` / `#concedeMatchBtn` (Bo1: `#concedeGameBtn` "Concede") → `#confirmConcede` (`#confirmConcedeTitle` names which; `#confirmConcedeYes`) → WS `concede_game` / `concede_match` | rule 650/651.1 | new — `match-flow.test.ts` (server) |
| Game-over interstitial (match continues) | `#gameOverOverlay.visible #gameOverBox[data-format=bo3][data-decided=0]` | `#goEyebrow` "Game N · Best of 3", `#goResult`, `#goMatchScore`, `#goContinueBtn` (→ WS `match_continue`; `#goWaiting`), `#goLeaveBtn` | 486.6; Continue from all humans (any one in sandbox) ⇒ `sync` with game N+1's pregame | new |
| Post-match | `#gameOverBox[data-decided=1]` | `#goMatchResult` "You win the match!" / "X wins the match" (+ conceded), `#goMenuBtn` (leave_game + menu), `#goRematchBtn` (→ WS `match_rematch`) | pushes `match_over {match}`; frames carry `match {format, gameNumber, score, games, decided, winner, …}` | new |

## Pregame — Sideboarding step (between games of a match; `apps/riftbound-app/public/js/gameplay/pregame.js` §Sideboarding, styles `public/css/sideboard.css`, server `server/pregame.ts` §Sideboarding)

Reach it: game 2+ of a Bo3 with a registered sideboard, `POST /api/game/create {…, sideboardBeforeGame1:true}`, or (sandbox) `GET /play/test?sideboard=1` (Tester seat gets a 9-card sideboard incl. extra copies of two main-deck spells). `?debugSideboard=1` on `/play` renders the step from a synthetic frame with no server (layout only; Lock just toasts).

| Affordance | Where | Selector(s) | Rule | Status (before → after) |
|---|---|---|---|---|
| Dense two-column **Main \| Side** lists, one row per DISTINCT card with a quantity | `#pregameOverlay.visible #pregameContent.sideboard-step #sbColumns.sb2` | `#sbMainList .sb-row[data-sb-col=main][data-sb-def=<defId>][data-def-id]` / `#sbSideList .sb-row[data-sb-col=side]…`; `.sb-row__cost`, `.sb-row__name`, `.sb-row__qty` ("×3", or "×3 → ×1" + `.sb-row__delta` "2 → side" / "+1 ← main" when changed, row gets `.sb-row--changed`; `.sb-row--zero` at ×0); sticky headers `.sb-col__head` with live counts `#sbMainCount`/`<mainSize>` and `#sbSideCount`/`<sideMax from /api/config deckRules>` (`.sb-count--bad` when off); hover a row ⇒ `#cardPreview.visible` beside the column | ~25 rows fit at 900px without an inner scrollbar (22px rows); thin dark scrollbar beyond | **was: chunky thumb rows sorted by cost that RE-SORTED and hopped columns on every swap; "x2" badge made it unclear whether one or two copies moved** → fixed, asserted (`server/__tests__/sideboard-ui.test.ts`: row set/order; browser: 40 main rows + 6 side rows on `/play/test?sideboard=1`) |
| **Stable order** — rows NEVER move or re-sort during the step | both lists | order fixed when the step starts (type unit→spell→gear, then cost, then name) from the ORIGIN tags in the instance ids (`-main-`/`-side-`), so it survives reconnects and earlier swaps; copies arriving in a column that has no row for that card render as GHOST rows appended at the bottom: `.sb-row.sb-row--ghost[data-sb-ghost=1]` "+2 Frigid Touch ← main" (a card registered in both columns just updates both real rows) | list index of every real row is identical before/after any number of adjustments | asserted (test: `rowOrder()` before == after, ghosts appended; browser: `sameOrder === true` after 4 adjustments) |
| Explicit **[−] [+] steppers** per row | `.sb-row .sb-row__steps` | `button.sb-step[data-sb-dir="-1"\|"1"]` — Main column: **−** sends ONE copy to the sideboard, **+** pulls one back / in (disabled at ×0 / when no copy of that card is left in the sideboard = "original + incoming" cap); Side column symmetric (− = one copy into the main deck). Clicking a row selects it (`.sb-row--sel`); drag a row onto the other column (`[data-sb-drop=main\|side]`, `.sb-drop-target`) = that row's − | quantities are edited locally; nothing is sent until Lock in | **was: click-a-main-row-then-a-side-row / drag-row-onto-row 1-for-1 swap sent immediately, list reshuffled** → replaced, asserted (test: `adjust`/`canAdjust` bounds, counts; browser: 3× − on the Side "Disintegrate ×3" row ⇒ `#sbMainCount` 42/40 in red + a "+3 Disintegrate ← side" ghost row at the bottom of Main) |
| **Swap summary + Reset + inline size validation** above Lock | under the columns | `#sbSwaps.sb-summary`: "Swaps (`#sbSwapCount`): `.sb-swap-chip--out` −1 Cleave, −1 Gust · `.sb-swap-chip--in` +3 Disintegrate" + `#sbResetBtn` (back to registered); `#sbValidity.sb-validity--bad` "Main deck is 41/40 — send 1 more card to the sideboard (swaps are 1-for-1)" / `--ok` "Sizes OK — main deck returns to 40" / `--warn` (sideboard over cap: advisory, never blocks); `#sbLockBtn.start-btn` "Lock in (3 swaps)" / "Lock in (no changes)", `disabled` + `title=<reason>` while main ≠ its size or after sending | main deck must return to its registered size (Σdelta = 0); sideboard size is then invariant | asserted (test: validity reason + disabled attr in HTML, enabled when balanced; browser: 42/40 ⇒ disabled, 40/40 ⇒ "Lock in (3 swaps)" enabled) |
| **Keyboard** | document keydown (capture) while `pregameState.phase === "sideboard"` and not locked | ↑/↓ move `.sb-row--sel` through Main rows, Main ghosts, Side rows, Side ghosts (scrolls into view); **−** / **←** = the selected row's −; **+** / **=** / **→** = its +; Esc clears the selection; ignored in inputs / with modifiers | — | asserted (browser: click row, ←, ↓, − ⇒ two rows changed) |
| **Lock in** sends ONE batch frame | `#sbLockBtn` click | `{type:"sideboard_lock", swaps:[{out:<instance id now in main>, in:<instance id now in side>}, …]}` computed from the quantity deltas against the server's CURRENT lists (prefers returning already-crossed instances, so Reset after a reconnect undoes exactly); server `applySideboardSwaps` applies the batch atomically then locks — a bad pair ⇒ `{type:"error", errorCode:"SIDEBOARD_LOCK"}` + fresh private sync, nothing applied, button re-armed; plain `{type:"sideboard_lock"}` and per-swap `{type:"sideboard_swap", out, in}` still accepted | opponent only ever sees choosing/locked | asserted (`pregame-sideboard.test.ts` "batch lock"; `sideboard-ui.test.ts` lock frame; browser: WS capture shows exactly one `sideboard_lock` with 3 swaps ⇒ `pregameState.phase === "mulligan"`, 4-card `#mulliganHandCards`) |
| Locked / spectator / practice | same step | after lock: read-only lists (no `.sb-step`, no `#sbLockBtn`) + `#sbWaiting` "Locked in. Waiting for opponent…" / "Both locked in — shuffling…"; spectator: "Spectating — waiting for both players to lock in…"; sandbox: `#sbSkipToggle` "Skip sideboarding in practice games" (`localStorage["rb-skip-sideboarding"]="1"` ⇒ auto-lock with no swaps once per step); pending quantities persist across a reload in `sessionStorage["rb-sb:<gameId>:<n>:<hash of instance ids>"]` | — | asserted (test: read-only HTML; skip toggle present when `pregame.sandbox`) |

## Decks page (`/decks`) — import + My Decks rows

| Affordance | Where | Selector(s) | Rule | Status |
|---|---|---|---|---|
| **Import from text** modal opens blank | `#btnImportPaste` → `#importOverlay.visible` | `#importText`, `#importName`, `#importResult` | all three are EMPTY on every open (reset on open and after a successful import); content survives only while a failed import is being fixed | was: textarea/name kept the previous paste → fixed, asserted (`decks-page.test.ts`: open→type→close→reopen ⇒ empty; reopen after success ⇒ empty) |
| Successful import saves, closes, lists, highlights, toasts | `#importGoBtn` → `POST /api/saved-decks/import` (the server saves; legality is advisory) | overlay loses `.visible`; new row `.deck-card.dc-new[data-deck-id]` scrolled into view + pulsed; `#toast.visible` title "Imported <name>", body "Legal ✓" or "⚠ n issues (not tournament-legal — still playable)" (+ "· k lines not recognized") | HTTP 200 ⇒ success even when not tournament-legal; HTTP 400 (empty / nothing recognized) or 401 ⇒ modal STAYS open with the paste and `#importResult.err` listing the error + unrecognized lines | was: modal stayed open with a text report and the list refreshed behind it → fixed, asserted |
| My Decks row = legend art card | `.deck-grid .deck-card[data-deck-id]` (also Public Decks when signed out) | `.dc-art .dc-legend-img` = `<img src="/card-image/<legendId>" alt="<legend name>">` cropped to the art box, `.dc-champ img` (chosen champion, small, overlaid bottom-right), hover `.dc-art` ⇒ `.dc-preview` full legend card; `.dc-name`, `.dc-meta` (format · STANDARD/PREVIEW · `.dc-legal` Legal ✓ / ⚠ Not tournament-legal (n) · tiny `.dc-pips` domain dots), `.dc-legend` "Legend · Champion", `.dc-counts` "<b>40</b> main · <b>8</b> side · <b>12</b> runes" (server `counts` on every `/api/saved-decks` row; main includes the champion's copy), `.dc-date` "Edited 5 minutes ago", `.dc-delete` | no `.domain-badge` colour-chip block any more; missing legend / 404 art ⇒ neutral ⚔ placeholder (`.dc-art-ph`), missing champion ⇒ no overlay | was: name + colour chips + text only → redesigned, asserted (img alt = legend, champion img present, 0 `.domain-badge`, counts line) |

## Layout invariants (DESIGN.md §Board layout) — asserted at 1440×900 and 1920×1080

| Invariant | How it holds now | Status |
|---|---|---|
| Legend/Champion always visible, never under runes (both players) | player hand/rune row has a content-independent basis (`flex: 0 1 264px`); `renderRuneStacks` compresses the fan to the row's real height; opponent pile is compact (70×98) and the opponent hand 80% size, which hands the height back to the player's row | was: pile clipped at the bottom / (earlier) painted over the legend → fixed, asserted (no overlap, no clipped rune, every rune hittable) |
| Rune-stack cards ≈ hand-card size | player runes stay 110×154 logical | OK |
| **Rune row is stable under the mouse** ([rule:ui-rune-row-stable]) | hovering a rune never changes ANY rune's layout box or stacking: no translate/scale/z-index on `:hover` (gold ring + brightness only); fan order = pool order (never re-sorted by exhausted state) so tapping/recycling one rune never moves a sibling; the tapped rune rotates 90° **inside its own slot** (`transform-origin: 0 0; translateX(132px) rotate(90deg)` → occupies the top 110px of its 154px slot across the 156px stack, never covering the strip of the rune above); `#player-runePool` / `#opponent-runePool` are **patched in place** (`patchInnerHTML`, keyed by `data-card-id`) instead of rebuilt, so the node under the cursor keeps `:hover` across state updates | was: hover = `translateY(-6px) scale(1.05)` + `z-index: 20 !important` (BELOW the siblings' inline 100+i) → hit-box jumped to the neighbour and the fan flickered; tap re-sorted the fan (siblings jumped 20px); every frame rebuilt the DOM → fixed, asserted in `ui-stability.test.ts` (rects identical after N hovers, hovered rune is `:hover` and topmost at the pointer, sibling rects identical after a real tap, DOM node identity preserved) |
| **Deck / rune / trash piles show what they are** ([rule:ui-pile-art]) | `renderDeckStack` draws the MAIN pile as the main card back (CSS-drawn `.card-back-art--main`: deep blue + rune sigil, no image asset) and the RUNE pile as the gold rune back (`.card-back-art--rune`), each with a count badge + label; `renderTrashStack` shows the **face of the most recent (top) trash card** (`img.deck-stack-top` + name strip + count; hover = preview of that card, click = zone viewer of the whole public trash); an empty pile is a dashed placeholder (`.deck-stack--empty`). Both players. The same drawn back is used for opponent hand backs and facedown cards | was: three identical grey boxes with a number → asserted in `ui-stability.test.ts` (backs present + distinct, counts = zone sizes for both seats, trash top = last trashed card's art, hover previews it, click opens the viewer, empty = placeholder) |
| **Hover preview never sticks** | the delegated `mouseover`/`mouseout` driver alone missed every case where the hovered node disappears WITHOUT a mouseout (a prompt closes and takes its rune/card tiles with it — the reported "stuck after hovering a rune while choosing runes for Targon's Peak" —, a `state_update` re-render replaces the node, a modal opens on top). `overlays.js` now re-validates the panel from its anchor: a `MutationObserver` (childList + class/style/hidden attributes), a rAF-throttled `pointermove` `elementFromPoint` check and a 400 ms poll all run `validatePreview` → hide when the anchor is detached / not rendered / no longer the card surface under the last pointer position (a new node in the same slot re-targets instead); plus hide on `pointerdown` (latched until the pointer really moves, so a click-triggered re-render can't pop it back), `Escape`, wheel/scroll, `mouseleave` of the document, window `blur`, `visibilitychange`, and an 8 s max-visible backstop | **was: panel stayed up until the next mouseover of another card** → asserted in `ui-stability.test.ts` (node removed under a still cursor, modal opened over it, Escape, click, real Stacked Deck prompt answered while hovering its tile → gone within 200 ms; re-hover shows it again) |
| **One row per battlefield side, however crowded** ([rule:ui-bf-one-row]) | `renderBfSide` stamps the slot count on the row (`.bf-units[data-n] --n`, `--stacks`); CSS gives every slot `--bfw = clamp(min(64px, natural), fair share of the row, natural)` so cards first **shrink**, and once at the 64 px floor a computed negative margin (`--bf-lap`) **overlaps** them like a fanned hand (later cards on top, `:hover` lifts one clear, each keeps a ≥ 16 px clickable strip — `_ui.hitPoint` aims for the visible strip when the centre is covered); `overflow-x: clip`, never a scrollbar, never a second line. A side with ≥ 4 slots is "crowded": Might is printed as a chip on the visible (left) strip (`.card-might-chip`, damage chip moves left too), names hide, the art column narrows to 150 px and the battlefield may widen to 560 px, and a `⤢ N` chip (`.bf-spread-chip`) opens the **spread view** `#bfSpread`: every card on that side at full 100×140 in a wrap grid (`.bf-spread-tile[data-spread-for]`, clones that are hover-preview surfaces and forward a click to the real board card → select / target pick), re-synced on every render, closed by `Esc`, outside click or `×`. Attackers (opponent side, top) and defenders (player side, bottom) stay separate rows split by the divider | **was: wrap → 3rd unit vanished below the box; then a nowrap strip that shrank to 34 px and scrolled sideways** → asserted in `ui-stability.test.ts` with 8 Recruit tokens a side (n=8 both rows, no scroll/wrap, none outside the row, every card has a clickable strip + Might chip, clickCard selects first/middle/last, spread = 8 tiles in viewport, tile hover previews, tile click selects the board card, Esc / outside click close) |
| **Attached gear sits under its unit** (rule 434.4) | `groupAttachments` folds each Equipment whose `meta.attachedTo` holder is in the same zone list into the holder's slot; `renderUnitSlot` renders `.unit-stack[data-stack-host]` = the unit on top + each gear as `.unit-stack-gear` offset so its right edge **peeks 14 px** (hover the edge → it slides out and previews; click = the gear's own bar, e.g. its activated ability); base and battlefields; a gear whose holder isn't in the list stays a loose card with the 🔗 badge | **was: unit and gear rendered as two loose cards with 🔗 name badges** → asserted in `ui-stability.test.ts` (Serrated Dirk equips under its unit in base: stack present, gear peeks right of the host, `elementFromPoint` on the peeking edge hits the gear) |
| **Hover preview on every card surface** ([rule:ui-hover-preview]) | one floating panel `#cardPreview` (`position: fixed; pointer-events: none; z-index 10050`) driven by a single delegated `mouseover`/`mouseout` listener over `.card[data-card-id]`, `.bf-art[data-card-id]` (battlefields), `.choice-modal-card` / `.prompt-source` (cards inside prompts), `.deck-stack[data-card-id]` (trash top), `.zone-viewer-card`; shows large art (**landscape 420px for battlefields**) + name + type/domain + **full rules text** (`#previewText`) + cost/might/state chips; placed beside the hovered element (right/left, above for hand rows; the whole rune stack counts as the anchor so the fan is never covered), clamped to the viewport; appears on mouseover, hides on mouseout (60ms bridge between adjacent cards); opponent hidden zones still refused outside sandbox | was: battlefields had no preview at all (`showPreview` looked the id up in zones and `.bf-art` had no handler — only the 240×340 sidebar slot showed a squeezed landscape image), other cards showed the image only (text nodes `display:none`), runes were excluded → fixed, asserted (battlefield / unit at a battlefield / legend / champion / hand / rune / prompt tile: name + rules text within 150ms, hidden after mouseout, fixed + pointer-events none) |
| Exhausted = rotate 90° + dark overlay everywhere | `.card.card--exhausted` in base **and** battlefield rows (asserted via computed transform + ::before); `monkey-drive.ts` stale `[id^="bf-"]` selector → `#battlefieldRow .battlefield .card.card--exhausted` | OK; driver fixed |
| Actions panel never clips an expanded group | `max-height: 340px` + `toggleMoveGroup` scrolls the group into view; asserted no clipped sub-button | 220px panel hid expanded rune options → fixed |
| Battlefield unit rows show whole cards | asserted the unit at a battlefield is not clipped by its row | OK |
| VP badge = effective victory score (Aspirant's Climb +1) | `victoryScoreEffective[pid]` from the server | OK (asserted "1 / 9") |
| Chain overlay shows item mode/targets | `[data-chain-what]` | fixed |
| Prompt modal never hidden behind zoom / hover preview | opening any prompt / play-options modal closes the zoom and the floating preview; zoom refuses to open over a modal | OK (asserted) |
| Whose-cursor banner | sidebar header names priority / focus / prompt holder; gold when it is ours on the opponent's turn | new |

## Known engine-side gaps surfaced by the audit (not UI)

* `activateAbility` for Blind Monk (`ogn-257-298`, cost `[1], [Exhaust]`) is enumerated with 0 energy in the pool — the UI can only grey out abilities the engine does *not* enumerate.
* `equipCard` is enumerated while the chain is open (rule 476.1 is a discretionary action; expected only at main-phase timing).
* `revealHidden` params carry no targets; a Hidden spell with targets is targeted by the follow-up prompt (fine, noted for the harness).
