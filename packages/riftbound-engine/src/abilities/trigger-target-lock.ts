/**
 * Folded into the trigger finalization dialog (`trigger-finalization.ts`,
 * rules 402.2 / 808.1.d.2). Kept as a re-export for existing importers.
 */
export { casterChosenTarget } from "./trigger-finalization";
